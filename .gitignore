# Compilation Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Application construite
dist/
*.app/

# Médias et sous-titres de test (jamais versionnés)
*.mp4
*.mov
*.m4v
*.srt
*.vtt

# Images de référence : extraites de vidéos réelles, donc hors dépôt au même
# titre que les vidéos elles-mêmes. Reproductibles par ./Scripts/images_reference.sh.
Images-reference/

# Corpus de vérification : des sous-titres réels, donc hors dépôt par principe.
# ./Scripts/verifier.sh l'utilise s'il existe, sinon on lui passe --corpus.
# Les fixtures des contrôles, elles, sont des chaînes Swift — rien à versionner.
Corpus/

# Profil personnel (réglage local, pas réglage du projet)
profil.json

# macOS
.DS_Store
._*

# Config locale de l'assistant
.claude/
